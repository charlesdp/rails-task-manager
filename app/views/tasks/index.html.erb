<h3>These are all the tasks you have for the moment</h3>

<ul>
  <% @tasks.each do |task| %>
  <li>
    <%= link_to task.name, task_path(task) %>
  </li>
  <%end%>
</ul>

<%= link_to 'Add a task', new_task_path %>